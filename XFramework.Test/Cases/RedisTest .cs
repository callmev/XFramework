using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace XFramework.Test.Cases
{
    [TestClass]
    public class RedisTest
    {
        [Ignore]
        [TestMethod]
        public void AddToRedis()
        {
            //var command = DataBuilder.PreCreateTargetCommand();
            //var key = command.TargetId;
            
            //RedisClientManager.Item_Set<CreateTargetCommand>(key, command, TimeSpan.FromSeconds(10));

            //var result = RedisClientManager.Item_Get<CreateTargetCommand>(key);

            //Assert.IsNotNull(result);
            //Assert.AreEqual(command.Topic, result.Topic);
        }
    }
}
